import 'package:flutter/material.dart';

class SuppliersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suppliers Page'),
      ),
      body: Center(
        child: Text('This is the Suppliers Page.'),
      ),
    );
  }
}
