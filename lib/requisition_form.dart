import 'package:flutter/material.dart';

class RequisitionFormScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Requisition Form'),
      ),
      body: Center(
        child: Text('This is the Requisition Form Screen'),
      ),
    );
  }
}
